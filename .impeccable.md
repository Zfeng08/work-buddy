# 设计上下文 · 珠峰工作台

> 本文件供 impeccable 等前端设计技能读取，避免产出泛化设计。修改视觉方向时同步更新。

## 产品

个人数据工作台（dashboard）。GitHub Actions 定时采集 → 仓库即数据源 → GitHub Pages 静态站。
模块：总览 / 资讯 / 行情 / 天气 / 岗位 / 剧集 / 趋势 / 知识库 / 图谱。

## 目标用户

**单人自用**：一名制造业供应链 / 计划方向的从业者，处于求职期，对「岗位」模块使用频率最高。
自用工具，无需考虑获客、转化、多人协作、权限分级。
（本文件会进公开仓库，**不要在此写任何个人身份信息**。）

## 使用场景

- 每天早上扫一眼总览：岗位有没有新的、天气、财经要闻。
- 求职期高频切到「岗位」，按城市 / 领域 / 级别三档筛选，点标题跳猎聘详情。
- 知识库做长文归档与关键词关联，偶尔用口令解锁私有笔记（解密在浏览器内完成）。
- 多为桌面浏览器长时段停留，手机端偶尔查看。
- 弱网 / 中国大陆网络环境：外部 CDN 只依赖 jsDelivr（echarts、字体）。

## 品牌人格

**极简编辑风（editorial minimal）—— 纸与墨。**
关键词：克制、精密、可信、有印刷品的秩序感；不要「AI 生成的科技感」。
界面应该像一本排得很好看的数据年报，而不是一个炫技的 SaaS 落地页。

珠峰 = 品牌锚点：峰形标记、冷静、高海拔的克制感。

## 视觉纲领

- **配色**：暖中性灰阶（chroma ≈ 0.004，hue 80）。
  深色 = 墨（`#0e0e0f` 底 / `#171614` 卡）；浅色 = 纸（`#f7f6f3` 底 / `#fffefc` 卡）。
  绝不用纯黑纯白。品牌色只有一支沉静蓝（深色 `#6e9bf0` / 浅色 `#2f5fb8`），
  只出现在焦点、链接、图表、当前 tab 的下划线上。
  红 / 绿是**语义色**：A 股习惯红涨绿跌，不可挪作装饰。
- **字体**：拉丁与数字用 Instrument Sans Variable（jsDelivr，30KB）；
  中文回落系统栈（PingFang SC → 鸿蒙 → 思源 → 微软雅黑）。不引中文 Web 字体（体积不可接受）。
  固定 rem 字阶（数据看板不用流体字号），数字一律 `tabular-nums`。
- **空间**：4px 基准；卡片内 24px；模块间 20px。靠留白和发丝线分层，不靠阴影堆叠。
- **形状**：圆角收到 `6px`（卡片）/ `4px`（小元素）/ 全圆仅用于筛选 chip。
- **质感**：无渐变、无外发光、无毛玻璃滥用（仅顶栏 backdrop-filter）。
  层级 = 1px 发丝边框（`--border`）+ 极轻 `--shadow-sm`；悬浮态才用完整阴影。
- **动效**：`cubic-bezier(.22,1,.36,1)`，160–300ms；只动 opacity / transform / 颜色。
  必须尊重 `prefers-reduced-motion`。

## 硬性约束

- `assets/app.js` 通过类名与 id 拼 HTML，**改版不得改动这些类名与 id**。
- 以下 CSS 变量名被 app.js 读取，必须保留：
  `--text --text-dim --border --bg-hover --accent --accent-rgb --accent-grad-b`，
  另需 `--red --accent2 --accent2-rgb --warn --warn-rgb --bg-card --bg --radius --shadow`
  供既有规则与 rg 透明色使用。
- 保留浅色 / 深色双主题，默认跟随系统，右上角可手动切换并写入 localStorage。
- 站点必须能在中国大陆网络下正常渲染（字体加载失败要优雅回落，不能阻塞首屏）。

## 反面清单（本项目禁止出现）

- 紫蓝渐变、霓虹发光、毛玻璃卡片、`◆` 之类的通用装饰符号。
- 万物皆卡片 + 卡片套卡片的层层嵌套。
- 大圆角（14px+）药丸按钮与渐变选中态。
- Inter / Roboto / Arial 等「隐形默认字体」。
